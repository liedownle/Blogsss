# 博客操作手册（大鹅的学习博客）

> 本手册用于日常使用本博客。请保存好，遇到问题先查这里。

---

## 一、博客基本信息

| 项目 | 内容 |
|------|------|
| 博客仓库 | `Blogsss` |
| 站点地址 | `https://liedownle.github.io/Blogsss/` |
| 托管方式 | GitHub Pages（自动构建部署） |
| 加密方式 | AES-256-CBC（构建时加密 + 前端解密） |
| 博客技术栈 | Jekyll + GitHub Actions |

**重要：本项目是"子路径"部署**，所有在文章正文里引用的图片/文件路径都要带 `/Blogsss/` 前缀（详见第五部分）。

---

## 二、日常发布一篇博客（完整流程）

### 第 1 步：创建博文文件

在 `myblog/_posts/` 目录下新建一个 Markdown 文件，文件名必须是：

```
年-月-日-标题.md
```

例如：`2026-08-28-我的第一篇.md`

### 第 2 步：写博文内容（头部模板）

```markdown
---
layout: post
title: "文章的标题"
date: 2026-08-28 10:00:00 +0800
categories: 分类名
# 想加密就加下面这一行，不加密就不写
encrypted: true
---

这里是文章正文……

## 小标题

写点内容。
```

### 第 3 步：上传图片（如果需要）

1. 在 `myblog/assets/images/` 目录放你的图片（没有就新建该文件夹）
2. 在文章里引用，**路径必须以 `/Blogsss/` 开头**：
   ```markdown
   ![图片说明](/Blogsss/assets/images/我的图片.png)
   ```

### 第 4 步：提交并推送（更新博客）

在 `myblog` 目录打开终端，执行：

```bash
git add .
git commit -m "新增文章：标题"
git push
```

### 第 5 步：等待部署

推送后 1~2 分钟，GitHub Actions 自动构建部署。打开仓库 **Actions** 页，看到绿色 ✓ 就成功了。
刷新你的博客站点即可看到新文章。

---

## 三、如何写加密博文

在文章的 frontmatter 里加一行 `encrypted: true` 即可：

```markdown
---
layout: post
title: "私密笔记"
date: 2026-08-28 11:00:00 +0800
encrypted: true    # ← 有这一行就是加密博文
---

正文内容……（构建时会被加密，仓库里看不到明文）
```

**加密相关注意：**
- 所有加密博文**共用同一个密码**（就是你在 GitHub Secrets 里设置的 `BLOG_SECRET_KEY`）
- 修改密码后，需要重新推送一次（触发重新构建）才能更新所有加密博文的密文
- 访客输入正确密码才能在浏览器看到内容，密码错误会提示

---

## 四、如何修改博客配置

打开 `myblog/_config.yml`：

| 配置项 | 说明 |
|--------|------|
| `title` | 博客名称 |
| `description` | 博客描述 |
| `url` | 站点地址（不要乱改） |
| `baseurl` | 子路径前缀 `/Blogsss`（**不要乱改**，改错整个站会挂） |
| `author.name` | 作者署名 |

改完保存后，重新 `git add . && git commit && git push` 生效。

---

## 五、如何新增友情链接（友链）

导航栏有"友链"页面，用来展示你朋友们的个人网站，添加/删除都很简单。

### 第 1 步：打开友链页面文件

`myblog/friends.md`，这就是"友链"页面的内容。

### 第 2 步：复制一份友链卡片并修改

当前文件里已有一位朋友的示例。想新增一位朋友，就复制一份下面的卡片粘贴进去（每位朋友一份）：

```html
<div class="friend-list">
  <a class="friend-card" href="https://朋友的网站/" target="_blank" rel="noopener">
    <span class="friend-name">朋友昵称</span>
  </a>
</div>
```

只需修改 2 处：

| 要改的位置 | 改成什么 |
|-----------|----------|
| `href="..."` | 朋友的网站完整链接（例如 `https://wd-2711.tech/`） |
| `朋友昵称` | 卡片上显示的名字 |

### 第 3 步：推送部署

```bash
git add .
git commit -m "add: friend link xxx"
git push
```

等 1~2 分钟构建完成后，打开 `https://liedownle.github.io/Blogsss/friends.html` 查看。

### 注意事项

- 导航栏的"友链"入口已经配好，**不需要**去改 `_includes/header.html`
- 友链是**纯文字卡片**（不带头像），想显示头像的样式请自行扩展
- 想删除某位朋友：删掉他对应的一整段 `<div class="friend-card">...</div>` 即可
- 对方网站打不开了，记得及时来删掉，避免死链

---

## 六、⚠️ 重要：路径规则（最容易出错的地方）

因为博客部署在子路径 `/Blogsss` 下，所以：

### 引用图片/文件

**❌ 错误写法**（缺少前缀，会 404）：
```markdown
![图](/assets/images/1.png)
[下载文件](/files/a.pdf)
```

**✅ 正确写法**（带 `/Blogsss/` 前缀）：
```markdown
![图](/Blogsss/assets/images/1.png)
[下载文件](/Blogsss/files/a.pdf)
```

### 引用文章/页面链接

在**文章正文**里链到博客内部页面时，同样要带前缀：
```markdown
[回到首页](/Blogsss/)
[关于我](/Blogsss/about.html)
```

### 记住规则

> **凡是 `/` 开头、链接到博客自己内容的路径，都要写成 `/Blogsss/xxx`。**
> `/Blogsss` 的 B 是**大写**，别写错。

### 为什么？

博客的完整地址是 `https://liedownle.github.io/Blogsss/`。如果你写 `/assets/...`，浏览器会去找 `https://liedownle.github.io/assets/...`，跳出了 `/Blogsss` 这个目录，自然找不到文件。

---

## 七、常见问题排查

### Q1：推完代码，网站没更新？
- 打开仓库 **Actions** 页，看任务是否失败（红叉）。
- 如果失败，点击失败的任务查看日志，通常能看出原因。
- 确认推送的分支是 `main`（不是 master）。

### Q2：文章点开是乱码或空白？
- 加密博文需要输入正确密码。
- 若提示"未正确加密"，说明构建时没读到 `BLOG_SECRET_KEY`，检查是否在 **Settings → Secrets** 里添加了。

### Q3：图片显示不出来（404）？
- 检查路径是否带 `/Blogsss/` 前缀（见第五部分）。
- 检查图片文件是否真的上传到了对应文件夹（`assets/images/`）。

### Q4：想修改加密密码？
1. 仓库 **Settings → Secrets and variables → Actions** → 编辑 `BLOG_SECRET_KEY`
2. 修改 `_config.yml` 或随便改个文件触发重新构建
3. 重新 `git push`，等 Actions 跑完即可

---

## 八、本地预览（可选）

需要本机安装 Ruby 才能本地预览，否则跳过（不影响线上发布）。

```bash
bundle config mirror.https://rubygems.org https://mirrors.aliyun.com/rubygems
bundle install
bundle exec jekyll serve
```

启动后打开 **http://localhost:4000/Blogsss/** 预览（注意子路径 `/Blogsss`，别漏了）。

> 国内网络直接 `bundle install` 可能因访问 rubygems.org 超时，先执行上面的 `bundle config mirror...` 配置阿里云镜像源即可。

---

## 九、项目目录速查

```
myblog/
├── _config.yml              # 博客配置（改这里改全局信息）
├── index.md                 # 首页（文章列表，通常不用改）
├── about.md                 # 关于页
├── friends.md               # 友情链接页（加好友改这里）
├── _posts/                  # ★ 你写文章的地方（Markdown）
│   └── 2026-08-28-xxx.md
├── assets/
│   ├── css/style.css        # 样式
│   ├── js/encrypt.js        # 前端解密脚本
│   ├── js/main.js           # 主题切换、阅读进度等交互脚本
│   └── images/              # ★ 放图片的地方
├── scripts/encrypt.js       # 构建时加密脚本（不要动）
├── .github/workflows/       # 自动部署配置（不要动）
└── README.md                # 部署说明
```

**你日常只需要关心 4 个地方：**
1. `_posts/` —— 写新文章
2. `assets/images/` —— 放图片
3. `_config.yml` —— 偶尔改配置
4. `friends.md` —— 偶尔加/删友链

---

## 十、个性化定制（换背景图 / 换主题色）

博客的外观样式都在 `assets/css/style.css` 里，改完保存后 `git push` 即可生效。

### 1. 更换首页背景图

背景图规则在 `style.css` 里（搜索 `.home-page::before` 可定位）：

```css
.home-page::before {
  background: url("../images/background_01.png") center / cover no-repeat;
  opacity: 0.5;
}
```

**换图方法（二选一）：**
- **方法 A（最简单）**：把新图片改名成 `background_01.png`，直接覆盖 `assets/images/background_01.png` 文件（注意图片格式要一致，比如都是 `.png`）。
- **方法 B**：把新图片放进 `assets/images/`，再把上面 `url("../images/...")` 里的文件名改成新图片名。

**常用参数：**

| 参数 | 作用 | 说明 |
|------|------|------|
| `opacity` | 透明度 | `0` 全透明 ~ `1` 不透明；`0.5` 即一半 |
| `cover` | 铺满并裁切 | 改成 `contain` 则完整显示（四周可能留白） |

### 2. 切换主题色（主色调）

主题色由 `style.css` 顶部的 CSS 变量控制：

```css
:root {                  /* 浅色模式 */
  --accent: #4f6ef7;     /* 主色（当前是靛蓝） */
  --accent-2: #8b5cf6;   /* 辅助色（当前是紫色） */
}

[data-theme="dark"] {    /* 深色模式 */
  --accent: #7c9bff;
  --accent-2: #a78bfa;
}
```

- `--accent` 是主色，影响：标题渐变、链接、按钮、标签、进度条、卡片悬停等。
- `--accent-2` 是辅助色，与主色一起组成渐变。

**换色方法**：把 `#4f6ef7`、`#8b5cf6` 换成你喜欢的颜色即可，建议浅色 / 深色两组一起改，保持一致。

> 颜色参考：绿色系 `#10b981` / `#34d399`；橙色系 `#f97316` / `#fb923c`；粉色系 `#ec4899` / `#f472b6`。

---

*最后更新时间：2026-09-14*
